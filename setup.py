from setuptools import setup


setup(
    name="platoon_gym",
    version="0.0.0",
    install_requires=["gymnasium", "numpy", "scipy", "cvxpy", "pytest", 
                      "matplotlib", "pygame", "pyyaml", "lark"]
)
